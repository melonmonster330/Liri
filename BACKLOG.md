# Liri Backlog

Items that are intentionally out of scope for now but worth revisiting.

---

## Listening History & Library Status Tracking
- `removed_at` / `is_active` flag on `user_library` so users can remove and re-add albums
- `last_played_at` per album
- Full listening history view in the UI

## BPM-Based Matching
- BPM is being stored at data-fetch time
- Future: use BPM as an additional matching signal during listen (e.g. tap tempo, beat detection)

## Non-Vinyl Mode
- App is vinyl-only for now by design
- Revisit if/when expanding to non-vinyl listening
