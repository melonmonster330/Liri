{
  "name": "liri",
  "version": "1.0.0",
  "description": "Lyrics that follow your record.",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required'",
    "sync": "npx cap sync",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
